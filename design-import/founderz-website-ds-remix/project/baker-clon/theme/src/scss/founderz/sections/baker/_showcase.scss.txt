// =============================================================================
// Baker Showcase (baker-showcase)
// =============================================================================

.baker-showcase {
    @include section-b;
    background: var(--bg-black);
    color: var(--white);
    padding-top: 0;

    &__grid {
        display: grid;
        grid-template-columns: repeat(6, 1fr);
        gap: 16px;

        @media all and (max-width: 810px) { grid-template-columns: 1fr 1fr; }
        @media all and (max-width: 580px) { grid-template-columns: 1fr; }
    }

    &__item {
        position: relative;
        border-radius: 24px;
        overflow: hidden;
        min-height: 240px;

        // 2 vídeos arriba (anchos), 3 imágenes abajo
        &--0, &--1 { grid-column: span 3; aspect-ratio: 16 / 11; }
        &--2, &--3, &--4 { grid-column: span 2; }

        @media all and (max-width: 810px) {
            &--0, &--1, &--2, &--3, &--4 { grid-column: span 1; aspect-ratio: 4 / 3; }
        }
        @media all and (max-width: 580px) {
            &--0, &--1, &--2, &--3, &--4 { grid-column: span 1; }
        }

        &__media { position: absolute; inset: 0; width: 100%; height: 100%; object-fit: cover; }
        &__overlay { position: absolute; inset: 0; background: linear-gradient(180deg, rgba(26,25,29,0.1), rgba(26,25,29,0.75)); }

        &__badge {
            position: absolute;
            top: 16px;
            right: 16px;
            display: inline-flex;
            align-items: center;
            gap: 6px;
            font-size: 13px;
            color: #fff;
            background: rgba(0, 0, 0, 0.4);
            padding: 6px 12px;
            border-radius: 100px;

            svg { width: 14px; height: 14px; }
        }

        &__caption {
            position: absolute;
            inset-inline: 20px;
            bottom: 20px;
            color: #fff;

            &__title { margin: 0; font-size: 20px; font-weight: 600; }
            &__sub { margin: 4px 0 0; font-size: 14px; color: rgba(255, 255, 255, 0.8); }
        }
    }

    &__cta { display: flex; justify-content: center; margin-top: 32px; }
}
