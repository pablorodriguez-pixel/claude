// =============================================================================
// Baker Testimonials (baker-testimonials)
// =============================================================================

.baker-testimonials {
    @include section-b;
    background: var(--grey);

    &__rating {
        text-align: center;
        margin-bottom: 16px;
        color: var(--black-opacity-7);

        span[aria-hidden] { color: #00b67a; letter-spacing: 2px; }
    }

    &__featured {
        background: var(--bg-black);
        color: var(--white);
        border-radius: 24px;
        overflow: hidden;
        display: grid;
        grid-template-columns: 260px 1fr;
        min-height: 280px;

        @media all and (max-width: 810px) { grid-template-columns: 1fr; }

        &__media {
            position: relative;
            background: rgba(255, 255, 255, 0.05);
            min-height: 220px;

            img { width: 100%; height: 100%; object-fit: cover; }
            .baker-testimonials__featured__play {
                position: absolute;
                top: 50%; left: 50%;
                transform: translate(-50%, -50%);
                width: 56px; height: 56px;
                border-radius: 50%;
                background: rgba(255, 255, 255, 0.92);
                display: grid; place-items: center;
                color: var(--black);
            }
        }

        &__body {
            padding: 32px;
            display: flex;
            flex-direction: column;
            justify-content: center;
            gap: 16px;

            blockquote { margin: 0; font-size: 18px; line-height: 1.5; }
        }

        &__author {
            strong { display: block; font-weight: 600; }
            span { font-size: 14px; color: var(--white-opacity-7); }
        }
    }

    &__grid {
        display: grid;
        grid-template-columns: repeat(3, 1fr);
        gap: 16px;
        margin-top: 16px;

        @media all and (max-width: 1024px) { grid-template-columns: repeat(2, 1fr); }
        @media all and (max-width: 810px)  { grid-template-columns: 1fr; }
    }
}
