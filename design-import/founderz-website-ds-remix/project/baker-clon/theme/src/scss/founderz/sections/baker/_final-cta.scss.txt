// =============================================================================
// Baker Final CTA (baker-final-cta)
// =============================================================================

.baker-final-cta {
    @include section-a;
    background: var(--purple);
    color: var(--white);

    &__wrapper {
        display: grid;
        grid-template-columns: 1fr 420px;
        gap: 56px;
        align-items: center;

        @media all and (max-width: 810px) { grid-template-columns: 1fr; gap: 32px; }
    }

    &__content { display: flex; flex-direction: column; gap: 20px; }

    &__label {
        font-size: var(--font-size-small);
        font-weight: 500;
        letter-spacing: 0.06em;
        text-transform: uppercase;
        color: var(--lilac);
    }
    &__title { font-size: var(--font-size-h2); font-weight: 600; line-height: var(--line-height-h2); color: var(--white); text-wrap: balance; }
    &__sub { font-size: var(--font-size-body); color: var(--white-opacity-7); }

    &__checks {
        list-style: none;
        margin: 8px 0 0;
        padding: 0;
        display: flex;
        flex-direction: column;
        gap: 12px;

        li { display: flex; gap: 10px; align-items: center; font-size: 16px; }
        span[aria-hidden] { color: var(--lilac); flex-shrink: 0; svg { width: 18px; height: 18px; } }
    }

    &__card {
        background: var(--white);
        color: var(--black);
        border-radius: 24px;
        padding: 28px;

        &__title { margin: 0 0 4px; font-size: var(--font-size-h3); font-weight: 600; }
        &__sub { margin: 0 0 16px; font-size: var(--font-size-small); color: var(--black-opacity-7); }
    }
}
