// =============================================================================
// Baker Hero + Dossier (baker-hero)
// =============================================================================

.baker-hero {
    @include section-a;
    position: relative;
    background: var(--bg-black);
    color: var(--white);
    padding-block: 32px 56px;

    &__top {
        display: flex;
        align-items: center;
        justify-content: flex-end;
        padding-bottom: 32px;
    }

    &__rating {
        display: inline-flex;
        align-items: center;
        gap: 8px;
        font-size: var(--font-size-small);
        color: var(--white-opacity-7);

        &__stars { color: #00b67a; letter-spacing: 2px; }
    }

    &__wrapper {
        display: grid;
        grid-template-columns: 280px 1fr 400px;
        gap: 40px;
        align-items: start;

        @media all and (max-width: 1024px) {
            grid-template-columns: 1fr 380px;
            gap: 32px;
        }
        @media all and (max-width: 810px) {
            grid-template-columns: 1fr;
            gap: 28px;
        }
    }

    &__reel {
        position: relative;
        border-radius: var(--acc-radius, 24px);
        overflow: hidden;
        aspect-ratio: 9 / 16;
        background: rgba(255, 255, 255, 0.05);

        @media all and (max-width: 1024px) { display: none; }

        &__media { width: 100%; height: 100%; object-fit: cover; }
        &__label {
            position: absolute;
            inset-inline: 12px;
            bottom: 12px;
            font-size: 13px;
            text-align: center;
            color: var(--white-opacity-7);
        }
    }

    &__content {
        display: flex;
        flex-direction: column;
        row-gap: 20px;
    }

    &__title {
        font-size: var(--font-size-h1);
        font-weight: 300;
        line-height: var(--line-height-h1);
        color: var(--white);
        text-wrap: balance;
    }

    &__meta {
        font-size: var(--font-size-body);
        color: var(--white-opacity-7);
    }

    &__bullets {
        display: flex;
        flex-direction: column;
        row-gap: 10px;

        &__title { font-weight: 600; color: var(--white); }

        ul { list-style: none; margin: 0; padding: 0; display: flex; flex-direction: column; row-gap: 10px; }
        li {
            display: flex;
            gap: 10px;
            align-items: flex-start;
            font-size: 16px;
            color: var(--white-opacity-7);
        }
        &__check { color: var(--lilac); flex-shrink: 0; svg { width: 18px; height: 18px; } }
    }

    &__card {
        background: var(--white);
        color: var(--black);
        border-radius: 24px;
        padding: 28px;
        box-shadow: 0 20px 60px rgba(26, 25, 29, 0.35);

        &__label {
            font-size: var(--font-size-small);
            font-weight: 500;
            letter-spacing: 0.06em;
            text-transform: uppercase;
            color: var(--purple);
        }
        &__title { margin: 6px 0 4px; font-size: var(--font-size-h3); font-weight: 600; }
        &__sub { font-size: var(--font-size-small); color: var(--black-opacity-7); margin-bottom: 16px; }
        &__note { font-size: var(--font-size-small); color: var(--black-opacity-7); text-align: center; margin-top: 12px; }
    }

    &__trust {
        margin-top: 48px;

        &__title { text-align: center; font-size: var(--font-size-small); color: var(--white-opacity-7); margin-bottom: 16px; }
        &__logos {
            display: flex;
            justify-content: center;
            align-items: center;
            gap: 40px;
            flex-wrap: wrap;

            img { max-height: 40px; width: auto; filter: brightness(0) invert(1); opacity: 0.8; }
        }
    }
}
