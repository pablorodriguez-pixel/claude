// =============================================================================
// Webinar Final CTA (webinar-final-cta)
// =============================================================================

.webinar-final-cta {
    @include section-a;
    background: var(--purple);
    color: var(--white);

    &__inner {
        display: flex;
        flex-direction: column;
        align-items: center;
        row-gap: 24px;
        text-align: center;
    }

    &__label {
        font-size: var(--font-size-small);
        font-weight: 500;
        letter-spacing: 0.06em;
        text-transform: uppercase;
        color: var(--lilac);
    }

    &__title {
        font-size: var(--font-size-h2);
        font-weight: 600;
        line-height: var(--line-height-h2);
        color: var(--white);
        text-wrap: balance;
    }
}
