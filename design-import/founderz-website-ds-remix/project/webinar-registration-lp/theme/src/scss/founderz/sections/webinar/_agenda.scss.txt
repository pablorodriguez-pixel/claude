// =============================================================================
// Webinar Agenda (webinar-agenda)
// =============================================================================

.webinar-agenda {
    @include section-b;
    background: var(--lilac);

    &__wrapper {
        display: grid;
        grid-template-columns: 1fr 1.3fr;
        gap: 56px;
        align-items: start;

        @media all and (max-width: 810px) {
            grid-template-columns: 1fr;
            gap: 32px;
        }

        &__left-items {
            display: flex;
            flex-direction: column;
            row-gap: 24px;
        }
    }

    &__list {
        margin: 0;
        padding: 0;
        list-style: none;
    }

    &__item {
        display: flex;
        gap: 20px;
        padding-block: 20px;

        & + & {
            border-top: 1px solid rgba(80, 69, 200, 0.18);
        }

        &__time {
            flex-shrink: 0;
            width: 56px;
            font-size: var(--font-size-small);
            font-weight: 600;
            color: var(--purple);
            font-variant-numeric: tabular-nums;
        }

        &__content {
            &__title {
                font-size: var(--font-size-h3);
                font-weight: 500;
                line-height: 1.3;
                color: var(--black);
            }

            &__description {
                margin-top: 6px;
                font-size: var(--font-size-body);
                line-height: var(--line-height-body);
                color: var(--black-opacity-7);
            }
        }
    }
}
