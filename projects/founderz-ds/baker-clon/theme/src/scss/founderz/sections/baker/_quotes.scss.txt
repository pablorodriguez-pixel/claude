// =============================================================================
// Baker Quotes "¿Te suena?" (baker-quotes)
// =============================================================================

.baker-quotes {
    @include section-b;
    background: var(--bg-black);
    color: var(--white);

    &__grid {
        display: grid;
        grid-template-columns: repeat(3, 1fr);
        gap: 16px;

        @media all and (max-width: 1024px) { grid-template-columns: repeat(2, 1fr); }
        @media all and (max-width: 810px)  { grid-template-columns: 1fr; }
    }

    &__item {
        display: flex;
        flex-direction: column;
        gap: 20px;
        margin: 0;
        padding: 28px;
        background: rgba(255, 255, 255, 0.05);
        border: 1px solid rgba(255, 255, 255, 0.1);
        border-radius: 24px;

        &__img { width: 56px; height: 56px; border-radius: 50%; object-fit: cover; }
        &__text { margin: 0; font-size: 18px; line-height: 1.5; color: var(--white); }
        &__after { margin: auto 0 0; font-size: 15px; font-style: italic; color: var(--lilac); }
    }
}
