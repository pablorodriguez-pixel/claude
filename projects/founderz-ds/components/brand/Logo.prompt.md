Founderz logo — the geometric "Founderz" wordmark, or the standalone F mark. Inherits `currentColor`, so set the parent's color (black on light, white on dark, purple as accent).

```jsx
<Logo height={28} />
<Logo variant="mark" height={36} color="#fff" />
```
