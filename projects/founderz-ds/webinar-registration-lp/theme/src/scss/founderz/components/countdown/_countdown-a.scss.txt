// =============================================================================
// Countdown (countdown-a)
// =============================================================================

.countdown-a {
    display: flex;
    gap: 8px;
    width: 100%;

    &__unit {
        flex: 1;
        display: flex;
        flex-direction: column;
        align-items: center;
        gap: 4px;
        padding: 10px 4px;
        border-radius: 12px;
        background: var(--white);
        border: 1px solid rgba(0, 0, 0, 0.05);

        &__value {
            font-size: 28px;
            font-weight: 600;
            line-height: 1;
            color: var(--purple);
            font-variant-numeric: tabular-nums;
        }

        &__label {
            font-size: 12px;
            letter-spacing: 0.04em;
            text-transform: uppercase;
            color: var(--black-opacity-7);
        }
    }

    // Variante sobre fondo oscuro / morado (CTA final)
    &--second {
        max-width: 440px;
        margin-inline: auto;

        .countdown-a__unit {
            background: rgba(255, 255, 255, 0.08);
            border-color: rgba(255, 255, 255, 0.1);

            &__value { color: var(--white); }
            &__label { color: var(--white-opacity-7); }
        }
    }
}
